# OptionGreek Frontend

Next.js UI. All market views call the FastAPI backend (`NEXT_PUBLIC_API_URL`, default `http://localhost:8000/api/v1`).

```bash
npm install
npm run dev
```

See the repo root `readme.md` and `docs/` for architecture and strategy specs.
